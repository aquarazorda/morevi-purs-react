import styled from 'styled-components';
import { el, el_, common } from 'Extra/StyledUtils';

export const sp = el(styled.span(common));
export const span_ = el_(styled.span``);

export const dv = el(styled.div(common));
export const div_ = el_(styled.div``);